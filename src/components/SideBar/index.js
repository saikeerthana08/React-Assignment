import Context from '.components/Context'
import './index.js'

const SideBar = () => {
  return (
    <Context.consumer>
      return(
      <h1>Side Bar</h1> )
    </Context.consumer>
  )
}

export default SideBar
